deployment:
  proxy:
    proxy_required: false
  external_network:
    cidr: 10.0.123.0/24
    gateway: 10.0.123.1
    start: 10.0.123.51
    end: 10.0.123.80
    network_type: flat  # or vlan
    #segmentation_id:
  user:
    remote_access_location: remote
    run_demo_setup: true
    username: demo
    password: demo
    cidr: 192.168.1.0/24
    nameservers: 10.0.123.1
    security_group_rules: true
software:
  juju:
    bootstrap_args:
      - --debug
      - --model-default=test-mode=true
      - --model-default=disable-telemetry=true
      # LP: #2065490
      - --model-default=logging-config=<root>=INFO;unit=DEBUG
      # LP: #2067451
      - --model-default=cloudinit-userdata=@./microk8s-config.yaml
  charms:
    # LP: #2059774 and possibly for other race conditions
    mysql-k8s:
      channel: 8.0/edge
    mysql-router-k8s:
      channel: 8.0/edge
